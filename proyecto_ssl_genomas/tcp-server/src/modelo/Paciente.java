
package modelo;

import java.time.LocalDate;

// Entidad de dominio Paciente (servidor genera patientId).
// Campos mínimos: patientId, fullName, documentId, age, sex, contactEmail, registrationDate,
// clinicalNotes, checksumFasta, fileSizeBytes, active.
public class Paciente {
    // TODO: definir campos, constructores, getters/setters
}
